# Agent Cube Configuration
# Customize models and CLI tools for each persona

version: "1.1.0"

# CLI tool to use for each model
# gemini CLI: npm install -g @google/gemini-cli
# Then authenticate: gemini (choose Login with Google)
# Note: Gemini CLI now supports session resume via --resume latest (v0.17+)
cli_tools:
  sonnet-4.5-thinking: cursor-agent
  gpt-5.1-codex-high: cursor-agent
  grok: cursor-agent
  gemini-2.5-pro: gemini
  gemini-3-pro: cursor-agent
  gemini-3-pro-preview: gemini

# Writer personas
writers:
  writer_a:
    name: "sonnet"
    model: "sonnet-4.5-thinking"
    label: "Writer A"
    color: "green"
    
  writer_b:
    name: "codex"
    model: "gpt-5.1-codex-high"
    label: "Writer B"
    color: "blue"

# Judge personas
judges:
  judge_1:
    model: "sonnet-4.5-thinking"
    label: "Judge Sonnet"
    color: "green"
    
  judge_2:
    model: "gpt-5.1-codex-high"
    label: "Judge Codex"
    color: "yellow"
    
  judge_3:
    model: "gemini-2.5-pro"
    label: "Judge Gemini"
    color: "magenta"

# Prompter agent (generates prompts for writers/synthesis)
prompter:
  model: "sonnet-4.5-thinking"

# Paths
paths:
  worktree_base: "~/.cube/worktrees"
  sessions_dir: ".agent-sessions"
  
# Behavior
behavior:
  auto_commit: true
  auto_push: true
  auto_update: true
  session_recording_at_start: true

