# Agent Cube Project Configuration

writers:
  writer_a:
    name: "codex"
    model: "gpt-5.1-codex-high"
    label: "Writer A"
    color: "magenta"

judges:
  judge_3:
    type: "cli-review"
    review_tool:
      name: "CodeRabbit"
      command: "coderabbit review --plain --type all --cwd {{worktree}}"
    orchestrator:
      model: "composer-1"
    label: "CodeRabbit (via Composer)"
    color: "magenta"
